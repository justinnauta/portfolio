// Container for a project's information/data
export default class ProjectInfo {
    constructor(imageName, title, oneLiner) {
        this.imageName = imageName;
        this.title = title
        this.oneLiner = oneLiner;
    }
}
